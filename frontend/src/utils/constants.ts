/**
 * Central game constants shared across all frontend components.
 * Mirror the defaults in the backend GameConfig.java.
 */

/** Maximum energy a player can hold at any time (carry-over cap). */
export const MAX_ENERGY_STORAGE = 10;

/** Maximum energy a player can receive in a single turn replenishment. */
export const MAX_ENERGY_PER_TURN = 6;

/** Starting HP for both players at match start. */
export const STARTING_HP = 100;
